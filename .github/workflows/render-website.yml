name: render-website

on:
  push:
  schedule:            
  - cron: '0 0 1 * *' # run on first day of every month at midnight
  workflow_dispatch:

jobs:
  build:
    runs-on: macOS-latest
    if: "!contains(github.event.head_commit.message, '[skip ${{ github.workflow }}]')"
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      GOOGLE_SA: ${{ secrets.GOOGLE_SA }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: r-lib/actions/setup-pandoc@v2
      
      - uses: r-lib/actions/setup-r@v2
      
      - name: Install spatial dependencies for terra R package
        run: |
          brew install gdal proj
          
      - uses: r-lib/actions/setup-renv@v2

      # - name: Set RENV_PATHS_ROOT
      #   shell: bash
      #   run: |
      #     echo "RENV_PATHS_ROOT=${{ runner.temp }}/renv" >> $GITHUB_ENV          
      # 
      # - name: Cache Renv packages
      #   uses: actions/cache@v3
      #   with:
      #     path: ${{ env.RENV_PATHS_ROOT }}
      #     key: ${{ runner.os }}-renv-${{ hashFiles('**/renv.lock') }}
      #     restore-keys: ${{ runner.os }}-renv-
      # 
      # - name: Restore Renv packages
      #   shell: Rscript {0}
      #   run: |
      #     if (!requireNamespace("renv", quietly = TRUE)) install.packages("renv")
      #     renv::restore()
    
      - name: Render Rmarkdown website
        run: |
          Rscript -e 'source("render_site.R")'

      - name: Commit results
        run: |
          git config --local user.name "$GITHUB_ACTOR"
          git config --local user.email "$GITHUB_ACTOR@users.noreply.github.com"
          git add -A && git commit -m "∆'s from Github Action ${{ github.workflow }} [skip actions]" || echo "No changes to commit"
          git push origin || echo "No changes to commit"
